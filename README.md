# VentureVibe
A trip focused social media app with a built in social network that identifies legs as "trips" and make planning and posting trips easier!

To localhost and test the backend in the root folder: uvicorn backend:app --reload

To localhost and run the frontend in the src folder run: npm run dev
